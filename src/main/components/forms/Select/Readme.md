Default Select:

    <Select
      field={{
        onFocus:() => {},
        onChange:() => {}}
      }
      label="Checkbox label"
      options={
        [{ value: 'one', label: 'One' },{ value: 'two', label: 'Two' }]
      }
    />
